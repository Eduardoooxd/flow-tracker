# Eduardo Couto Next.js Template

## Getting Started

First, run the development server:

```bash
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser.

## E2E Tests

First Install the Browsers drivers:

```
pnpm exec playwright install
```

Then run the E2E Tests:

```
pnpm e2e
```

## Commit Rules

<TYPE_OF_COMMIT>: <COMMIT_MESSAGE>
